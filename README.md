# Django Project

A Django-based web application designed to manage users, categories, tags, and expenses with full CRUD functionality.  
This project follows Django best practices, modular structure, and clean code principles.

---

## 🚀 Features

- User registration, login, logout (Django Auth)
- Add, edit, delete expenses
- Categories (One-to-Many with expenses)
- Tags (Many-to-Many with expenses)
- Pagination + filtering
- Django admin panel enabled
- Bootstrap-based UI
- Secure routes using `login_required`

---

## 🛠️ Technologies Used

- **Python 3.x**
- **Django 4.x/5.x**
- **SQLite / PostgreSQL / MySQL** (configurable)
- **HTML, CSS, Bootstrap**
- **Django ORM**

---

## 📂 Project Structure
------------------------------

```bash
python manage.py runserver
```

```bash
python manage.py add_tags
```

```bash
python manage.py add_categories
```

```bash
python manage.py add_expenses
```